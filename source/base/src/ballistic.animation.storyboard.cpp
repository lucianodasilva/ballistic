#include "ballistic.animation.storyboard.h"

namespace ballistic {
	namespace animation {

		storyboard::storyboard (id_t id) :
			iresource (id) {}

		void storyboard::update (real game_time, real frame_time) {

		}

	}
}